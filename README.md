# Password-Strength-Checker
A Python CLI tool that checks password strength with a 0–100% score, visual strength meter, and personalized tips (length, uppercase, digits, special characters, common-pattern detection) to help you build stronger passwords.
